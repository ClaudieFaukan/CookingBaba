{% extends 'base.html.twig' %}

{% block title %}Toutes les recettes{% endblock %}

{% block body %}
Lorem, ipsum dolor sit amet consectetur adipisicing elit. Quisquam ea accusamus laborum rem repellendus accusantium in labore esse explicabo qui cum unde ullam, aut, sapiente quia provident, perspiciatis voluptatem deleniti?

<ul>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for recipe in recipes %}
            <tr>
                <td>{{ recipe.id }}</td>
                <td>{{ recipe.title }}</td>
                <td>{{ recipe.content|length > 100 ? recipe.content|slice(0, 100) ~ '...' : recipe.content }}</td>
        
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucune recette trouvée.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
